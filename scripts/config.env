# Self-Awareness Skill 配置文件
# 
# 使用说明：
# 1. 此文件定义默认路径，可被环境变量覆盖
# 2. 推荐：由Agent通过环境变量/参数指定自己的配置位置
# 3. 此文件仅作为fallback

# ========================
# 基础配置
# ========================

# Agent配置文件根目录
AGENTS_ROOT="${AGENTS_ROOT:-$HOME/.agents}"

# Agent数据目录（存放每个Agent的认知文件）
AGENTS_DATA_DIR="${AGENTS_DATA_DIR:-$AGENTS_ROOT/agents}"

# 全局基础人格文件（所有Agent的默认人格）
GLOBAL_PERSONA="${GLOBAL_PERSONA:-$AGENTS_ROOT/GLOBAL.md}"

# 默认Agent名称
DEFAULT_AGENT="${DEFAULT_AGENT:-default}"

# ========================
# 常见Agent/工具的默认路径
# 这些仅作为fallback使用
# ========================

# OpenClaw
OPENCLAW_DIR="${OPENCLAW_DIR:-$HOME/.openclaw/workspace}"

# Claude Code
CLAUDE_DIR="${CLAUDE_DIR:-$HOME/.claude}"

# ========================
# Agent特定配置（推荐使用方式）
# ========================
#
# 格式：AGENT_WORKSPACE_<agent_name>="/path/to/workspace"
# 示例：
#   AGENT_WORKSPACE_main="$HOME/.openclaw-autoclaw/workspace"
#   AGENT_WORKSPACE_researcher="$HOME/.openclaw/workspace"
# 
# 或者在Agent启动时动态设置：
#   export AGENT_WORKSPACE_main="$HOME/.openclaw-autoclaw/workspace"
#   bash auto-init-cognition.sh main
